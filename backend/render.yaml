services:
  - type: web
    name: laravel-api
    runtime: php
    rootDir: backend

    buildCommand: |
      composer install --no-dev
      php artisan config:cache

    startCommand: |
      php artisan serve --host=0.0.0.0 --port=$PORT